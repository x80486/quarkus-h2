pluginManagement {
  plugins {
    id("io.quarkus").version("${getProperty("quarkus.version")}")
  }

  repositories {
    // mavenLocal() // Uncomment when needed
    gradlePluginPortal()
    mavenCentral()
  }
}

rootProject.name = "quarkus-h2"
